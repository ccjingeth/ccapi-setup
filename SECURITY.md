# 安全说明

- API Key 默认只存储在系统密钥存储中。
- 配置导出默认使用 `YOUR_API_KEY`，只有用户主动勾选才导出完整密钥。
- 自动写入前创建带时间戳的备份，并使用临时文件原子替换。
- 应用只检查已声明的工具配置路径，不扫描整个磁盘。
- 诊断信息不记录 Authorization、Bearer、token、password、secret 或完整 API Key。
- 视频真实测试会产生上游请求，界面会单独确认。
